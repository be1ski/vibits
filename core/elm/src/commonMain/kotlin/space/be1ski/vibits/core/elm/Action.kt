package space.be1ski.vibits.core.elm

/**
 * Marker interface for TEA (The Elm Architecture) actions.
 *
 * All action types in TEA features must implement this interface.
 * This ensures type safety and restricts usage of flow helpers
 * to proper TEA context only.
 */
interface Action
