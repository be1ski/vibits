# AGENTS (desktopApp)

## Scope

Desktop wrapper for shared UI.

## Required change flow

Follow the root `AGENTS.md` flow. Always verify with:

```bash
./gradlew :desktopApp:run
```

## Notes

- Desktop app should remain thin; UI lives in `shared`.
