# desktopApp

Desktop entry point for the shared KMP UI.

## Key files

- `src/desktopMain/kotlin/.../DesktopMain.kt` - Compose Desktop launcher

## Run

```bash
./gradlew :desktopApp:run
```

## Notes

- All UI lives in `shared` module.
